<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/ll_image_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="20dp">

        <ImageView
            android:id="@+id/iv_invite_icon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@drawable/ic_game_invite"
            android:scaleType="fitCenter" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_invite_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/ll_image_container"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="15dp"
        android:gravity="center"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="15dp"
        android:textColor="@android:color/black"
        android:textSize="18sp" />

    <!--

    <TextView
        android:id="@+id/tv_accept"
        style="@style/tv_dialog_yes_no"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="40dp"
        android:layout_toEndOf="@id/tv_dialog_center"
        android:background="@drawable/dialog_tv_yes_background"
        android:text="Accept" />

    <TextView
        android:id="@+id/tv_dialog_center"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_below="@id/tv_invite_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/tv_deny"
        style="@style/tv_dialog_yes_no"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="20dp"
        android:layout_toStartOf="@id/tv_dialog_center"
        android:background="@drawable/dialog_tv_no_background"
        android:text="Deny" />
        -->

</RelativeLayout>